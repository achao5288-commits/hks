"""
UI Automation Adapter — Reserved Interface
===========================================
For cross-application UI automation using image recognition (OpenCV)
and OCR (Tesseract) to interact with real desktop applications.

This directory is a placeholder for future implementation.
Current implementation uses API/library-based executors instead.

Planned modules:
  - screen_capture.py  — Screenshot specific application windows
  - element_finder.py  — Locate UI elements via template matching
  - ocr_reader.py      — Extract text from screen regions
  - action_executor.py — Simulate mouse/keyboard interactions
  - app_adapters/      — Per-application adapters (Browser, Excel, WeChat, etc.)
"""
